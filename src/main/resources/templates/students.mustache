{{> fragments/header}}
{{> navbar}}

<div class="container">
    <table class="table">
        <thead>
        <tr>
            <th scope="col">Album</th>
            <th scope="col">Imie</th>
            <th scope="col">Nazwisko</th>
            <th scope="col">Mail</th>
        </tr>
        </thead>
        <tbody>
        {{#studentList}}
            <tr>
                <th><a href="/student/{{albumNumber}}">{{albumNumber}}</a></th>
                <td>{{name}}</td>
                <td>{{lastName}}</td>
                <td>{{mail}}</td>
            </tr>
        {{/studentList}}
        </tbody>
    </table>
    <p>łącznie studentów: {{studentCount}}</p>
</div>

{{> fragments/footer}}
